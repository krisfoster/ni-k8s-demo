apiVersion: apps/v1
kind: Deployment
metadata:
  name: demonative-deployment
  namespace: apps
  labels:
    app: demonative
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demonative
  template:
    metadata:
      labels:
        app: demonative
    spec:
      containers:
        - name: demonative
          image: phx.ocir.io/oracledeveloper/k8s-perf-demo:native.0.0.1-SNAPSHOT
          imagePullPolicy: Always
          resources:
            requests:
              memory: "128Mi"
              cpu: "2000m"
            limits:
              memory: "256Mi"
              cpu: "2000m"
          ports:
            - containerPort: 8080
              name: native-port

---
apiVersion: v1
kind: Service
metadata:
  name: demonative-svc
  namespace: apps
  labels:
    app: demonative
  annotations:
    oci.oraclecloud.com/load-balancer-type: "lb"
    prometheus.io/path: "/actuator/prometheus"
    prometheus.io/port: "8080"
    prometheus.io/scrape: "true"
spec:
  type: LoadBalancer
  ports:
    - port : 8080
  selector:
    app: demonative

---
